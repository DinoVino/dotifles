function workspaceExpansion(modelState) {
  if(modelState == true){
    var xScale = 1.5;
    console.log(modelState);
    return xScale;
  }else{
    var xScale = 1;
    console.log(modelState);
    return xScale;
  }
}
